# Задачи: <Название изменения>

**Идентификатор**: `<id>-<slug>`
**План**: [plan.md](plan.md) · **Спека**: [spec.md](spec.md)

<!--
Правила задач:
- [R#] — требование из spec.md, которое закрывает задача (трассировка обязательна;
  задача без [R#] допустима только в «Фундаменте» и «Полировке»).
- [P] — можно выполнять параллельно (разные файлы, нет зависимостей).
- Порядок = зависимости, не «важность»; среди равных — рискованное раньше (fail fast).
- Задача ≤ 5 файлов и выполнима за одну фокус-сессию; крупнее — дели.
- Проверка = исполняемая команда или наблюдаемое поведение, а не «работает».
- Текст задачи самодостаточен: исполнитель (человек или субагент) не видит этот чат —
  в тексте должны быть файлы, поведение и крайние случаи.
-->

## Фаза 1: Фундамент

<!-- Общая подготовка, без которой задачи требований не начинаются:
схемы, каркас, конфигурация. Не раздувать. -->

- [ ] T1. <что сделать, какие файлы> — проверка: `<команда>`

## Фаза 2: Требования

<!-- Группируй по требованиям в порядке важности; внутри требования — сначала
тесты (если проект их просит), потом код. Тестовая задача — та же [R#]. -->

- [ ] T2. [R1] <что сделать: файлы, поведение, крайние случаи> — проверка: `<команда>`
- [ ] T3. [P] [R1] <...> — проверка: `<команда или наблюдение>`
- [ ] T4. [R2] <...> — проверка: `<команда>`

## Фаза 3: Полировка

- [ ] T5. [R*] <документация, чистка> — проверка: `<команда>`
- [ ] T6. Прогнать все критерии приёмки из spec.md — проверка: отчёт по каждому A#

## Контрольные точки

<!-- Где останавливаемся и показываем результат пользователю. -->

- После фазы 2: <какой сценарий можно прожить руками / что демо>
