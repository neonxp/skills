# Спецификация: <Название изменения>

**Идентификатор**: `<id>-<slug>`
**Домен**: <kebab-case область системы, напр. `auth` — по нему дельта вольётся в `.specs/_system/`>
**Статус**: черновик <!-- черновик | согласовано -->

<!--
Правила этой спеки:
- Одно требование — одно наблюдаемое поведение. ДОЛЖНА/ДОЛЖЕН — обязательно;
  СЛЕДУЕТ — допускает обоснованное отступление; МОЖЕТ — опционально.
- Никакой реализации: «как» — в plan.md. Схема таблиц, библиотеки, очереди — не сюда.
- У каждого требования хотя бы один сценарий «Дано/Когда/Тогда», который реально
  его проверяет, а не пересказывает другими словами.
- Крайние и ошибочные случаи обязательны: пустой ввод, отказ зависимости,
  повторное действие. Happy path очевиден сам по себе.
- Все [УТОЧНИТЬ] закрыть до статуса «согласовано».
-->

## Добавляется

<!-- Новое поведение, которого не было. Если спека не меняет существующее
поведение системы — секции «Изменяется» и «Удаляется» удали. -->

### R1. <Название требования>

Система ДОЛЖНА <одно наблюдаемое поведение, проверяемое человеком вне кода>.

**Сценарий S1: <название конкретного кейса>**

- Дано: <исходное состояние>
- Когда: <действие или событие>
- Тогда: <наблюдаемый результат>

**Сценарий S2: <крайний/ошибочный кейс>**

- Дано: <...>
- Когда: <...>
- Тогда: <...>

## Изменяется

<!-- Уже описанное в .specs/_system/<домен>.md поведение. Приведи ПОЛНЫЙ новый
текст требования (номер сохрани или дай новый) и одну строку — что изменилось. -->

### R2. <Название требования> (было: <старая суть одной строкой>)

Система ДОЛЖНА <новое поведение>.

**Сценарий S3: <...>**

- Дано: <...>
- Когда: <...>
- Тогда: <...>

## Удаляется

<!-- Требование уходит: R# + одна строка, почему. -->

- R3. <Название> — <почему удаляется>

## Критерии приёмки

<!-- Измеримые, техно-нейтральные, каждое ссылается на требования.
Именно их прогоняют на spec-5-verify перед закрытием изменения. -->

- [ ] A1. <проверяемое утверждение> (R1)
- [ ] A2. <проверяемое утверждение> (R2, R3)

## Открытые вопросы

<!-- Ответ получен — пункт удали. Пустой раздел — удали раздел. -->

- [УТОЧНИТЬ: <самый важный вопрос>]
