---
title: Имя проекта
markmap:
  colorFreezeLevel: 2
  initialExpandLevel: 3
  maxWidth: 300
---

# Имя проекта — чем является система

## Слои и домены

### Домен (стек)

#### `модуль` — ответственность

##### ИмяМодели (БД: таблица)

- id (string) — идентификатор
- name (string) — отображаемое имя
- created_at, updated_at (timestamp) — служебные метки

## Ключевые потоки

### Название потока

- вход → преобразование → хранилище → выход

## Кросс-функциональное

### Область (авторизация, наблюдаемость, тесты…)

- деталь

## Структура файлов

### Корень

- `README.md` — назначение проекта и запуск
- `go.mod` — модуль и зависимости

### `internal/модуль/`

- `handler.go` — HTTP-обработчики
- `service.go` — бизнес-логика
- `icons/*.svg` (40 шт.) — набор иконок интерфейса
